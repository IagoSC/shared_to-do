import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    BarContainer: {
        flex: 1,
        marginVertical: 10,
        height: 50,
        paddingHorizontal: 10,
        flexDirection: "row",
        alignItems: "center",
    }
})