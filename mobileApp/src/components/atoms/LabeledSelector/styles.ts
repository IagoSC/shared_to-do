import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    TextInput: {
        borderWidth: 1,
        borderColor: "#999",
        borderRadius: 10,
        padding: 3,
        paddingHorizontal: 10,
        margin: 5,
    },
    label: {
        fontWeight: "bold",
        fontSize: 15,
        marginHorizontal: 10 
    }
})